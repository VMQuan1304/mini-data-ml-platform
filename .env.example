# Deliberately fake local-only values. Never reuse them outside this lab.
PLATFORM_PROFILE=core
LOG_LEVEL=INFO
POSTGRES_DB=platform
POSTGRES_USER=platform
POSTGRES_PASSWORD=local-dev-only
DATABASE_URL=postgresql://platform:local-dev-only@postgres.platform.svc.cluster.local:5432/platform
KAFKA_BOOTSTRAP_SERVERS=kafka.platform.svc.cluster.local:9092
KAFKA_TOPIC=transactions.v1
KAFKA_GROUP_ID=transaction-ingestion-v1
MINIO_ENDPOINT=http://minio.platform.svc.cluster.local:9000
MINIO_ACCESS_KEY=minio-local
MINIO_SECRET_KEY=minio-local-dev-only
MINIO_RAW_BUCKET=raw
MINIO_CURATED_BUCKET=curated
MLFLOW_TRACKING_URI=http://mlflow.platform.svc.cluster.local:5000
MODEL_URI=models:/risk-model@champion
PGVECTOR_DSN=postgresql://platform:local-dev-only@pgvector.team-ml.svc.cluster.local:5432/knowledge

